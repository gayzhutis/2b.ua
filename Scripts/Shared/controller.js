///*
//* Portal's controller base class.
//*/
define(function () {

    var Controller = function () {
        /*
        * Must be called from constructor with passed <paramref name="settings"/>
        */
        function initialize(settings) {
        }
    };

    return Controller;
});